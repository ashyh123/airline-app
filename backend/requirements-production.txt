# Optional production upgrade: FastAPI provides OpenAPI, validation and ASGI deployment.
# The supplied demo server intentionally uses only the Python standard library.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
ortools>=9.10,<10.0
